{
  "config": {
    "step": {
      "user": {
        "title": "Cookidoo → Google Keep Sync",
        "description": "Select source and target lists and optionally a conversation agent (e.g. Claude) for bulk classification of unknown items. The integration will automatically create a todo list 'Cookidoo Keep Categories' whose order defines the supermarket sort order.",
        "data": {
          "cookidoo_entity": "Cookidoo shopping list (source)",
          "keep_entity": "Google Keep list (target)",
          "conversation_agent": "Conversation agent (e.g. Claude)",
          "use_llm": "Enable LLM fallback"
        }
      }
    },
    "abort": {
      "already_configured": "This source/target combination is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Cookidoo → Keep options",
        "menu_options": {
          "entities": "Lists & agent",
          "learned": "Learned mappings (cache)",
          "advanced": "Advanced"
        }
      },
      "entities": {
        "title": "Lists & agent",
        "description": "Leave 'Categories list (override)' empty to use the auto-created 'Cookidoo Keep Categories' list.",
        "data": {
          "cookidoo_entity": "Cookidoo shopping list (source)",
          "keep_entity": "Google Keep list (target)",
          "categories_entity": "Categories list (override, optional)",
          "conversation_agent": "Conversation agent",
          "use_llm": "Enable LLM fallback"
        }
      },
      "learned": {
        "title": "Learned mappings",
        "description": "LLM-learned (or manually maintained) item→category mappings. One per line: 'item-name = Category name'. Manual entries override the LLM since the cache is checked before any classifier runs. Currently: {count} entries.",
        "data": {
          "text": "Mappings"
        }
      },
      "advanced": {
        "title": "Advanced",
        "description": "Number of learned examples per category we attach to the bulk LLM prompt to calibrate it on your schema. 0 = no examples; higher = more stable classification but more tokens per call.",
        "data": {
          "llm_examples_per_category": "Examples per category to LLM"
        }
      }
    }
  },
  "entity": {
    "todo": {
      "categories": {
        "name": "Cookidoo Keep categories"
      }
    }
  },
  "services": {
    "sync": {
      "name": "Sync Cookidoo → Keep",
      "description": "Sync the Cookidoo shopping list to the Google Keep list."
    },
    "reset_learned": {
      "name": "Reset learned mappings",
      "description": "Clears the LLM-learned cache."
    }
  }
}
